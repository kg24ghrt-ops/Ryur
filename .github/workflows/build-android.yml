name: Build Debug APK + Package Project

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build Android App (Debug)
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Set up JDK 21 (latest LTS)
      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 21

      # 3. Install latest Gradle
      - name: Install Latest Gradle
        uses: gradle/gradle-build-action@v2
        with:
          gradle-version: 8.6.1   # AGP 8.6.1 compatible
          wrapper-enabled: false

      # 4. Cache Gradle dependencies
      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper/
          key: gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-

      # 5. Verify library JAR exists
      - name: Verify Library JAR
        run: |
          if [ ! -f "./orchestrator-core/libs/orchestrator-core-1.0.0.jar" ]; then
            echo "Library JAR not found!"
            exit 1
          fi

      # 6. Build APK using system Gradle
      - name: Build Debug APK
        run: gradle assembleDebug --warning-mode all

      # 7. Run unit tests (optional)
      - name: Run Unit Tests
        run: gradle testDebugUnitTest --warning-mode all

      # 8. Package project + APK
      - name: Package Project
        run: |
          mkdir -p build/output
          ZIP_NAME=OrchestratorAI-Debug.zip
          cd ..
          zip -r "$ZIP_NAME" "$(basename $GITHUB_WORKSPACE)" -x "**/build/**"
          mv "$ZIP_NAME" "$GITHUB_WORKSPACE/build/output/"

      # 9. Upload APK + ZIP
      - name: Upload APK + ZIP
        uses: actions/upload-artifact@v4
        with:
          name: OrchestratorAI-Debug
          path: |
            app/build/outputs/apk/debug/app-debug.apk
            build/output/OrchestratorAI-Debug.zip