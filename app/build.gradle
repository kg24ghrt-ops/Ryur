plugins {
    id 'com.android.application'
}

android {
    compileSdk 34

    namespace "com.orchestrator.app" // <--- ADD THIS

    defaultConfig {
        applicationId "com.orchestrator.app"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildFeatures {
        viewBinding true
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

repositories {
    google()
    mavenCentral()
    flatDir { dirs '../orchestrator-core/libs' } // Path to your JAR
}

dependencies {
    implementation fileTree(dir: '../orchestrator-core/libs', include: ['*.jar']) // your JAR
    implementation 'androidx.core:core-ktx:1.12.0'
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'com.google.android.material:material:1.10.0' // Material 3
    implementation 'androidx.browser:browser:1.6.0'                // Custom Tabs
}