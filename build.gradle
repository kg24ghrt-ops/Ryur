buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.3.1' // matches Gradle 8.5
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        flatDir { dirs 'orchestrator-core/libs' }
    }
}